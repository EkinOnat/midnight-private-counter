# Codex Prompt - Midnight Builder Challenge Level 3

> Historical record: this is the prompt used for the approved July submission.
> The live project migrated from Preprod to Preview on August 4, 2026; current
> deployment details are maintained in `README.md` and `.env.production`.

You are helping me complete Level 3 of the Midnight Builder Challenge on Rise In.

Work directly in this existing Level 2 repository:

`C:\Users\ekino\OneDrive\Desktop\StartinAgain\Midnight_Project`

Project details:

- GitHub repository: `https://github.com/EkinOnat/midnight-private-counter`
- Network: Midnight Preprod
- Preprod contract address: `516b830d25b61b83abd63488618a8dc45e4aecc1a04da18377e467792bdeed62`

## How to work

Complete the implementation, not just a review or a set of instructions.

Before editing:

1. Read any applicable `AGENTS.md` instructions.
2. Inspect the repository, `git status`, `package.json`, existing tests, generated contract output, frontend, README, and workflow files.
3. Preserve the working Level 2 implementation and all unrelated user changes.
4. Use the connected Midnight documentation tools whenever Midnight APIs, Compact syntax, compiler behavior, privacy semantics, or supported versions are uncertain. Do not run Claude-specific MCP commands.
5. Treat the structure below as a required logical structure, but adapt it to the repository that already exists. Do not replace correct existing files merely because their organization differs slightly.
6. Do not hand-edit generated files under `managed/`; generate them with the repository's Compact compile command.
7. Never expose or commit wallet material, private keys, seeds, private witnesses, local deployment state, or secrets.
8. Do not deploy, push, create commits, or open a pull request unless I explicitly ask. You may prepare the repository and give me the commands or commit plan.

Work autonomously and make reasonable decisions from the repository. Ask me a question only if a missing choice would materially change the product or if a required external action needs my authorization.

Run the relevant verification commands after making changes. If a command cannot run because of a genuine environment limitation, report the exact command, error, and remaining manual step. Never claim a check passed unless you ran it successfully.

## 1. Inspect and complete the project structure

Verify that the repository contains the equivalent of:

```text
Midnight_Project/
|-- contracts/
|   `-- counter.compact
|-- managed/
|   `-- counter/                 # generated by the Compact compiler
|-- src/
|   |-- components/
|   |-- hooks/
|   |-- App.tsx
|   `-- main.tsx
|-- tests/
|   `-- counter.test.ts
|-- .github/
|   `-- workflows/
|       `-- ci.yml
|-- PROPOSAL.md
|-- README.md
|-- package.json
`-- package-lock.json
```

Create only genuinely missing required files or directories. If the repository has a valid equivalent structure, keep it. Confirm that the generated contract artifacts are reproducible with `npm run compile`.

## 2. Contract and privacy tests

Review and improve `tests/counter.test.ts`. There must be at least three meaningful, independent tests covering:

1. **Circuit logic:** the circuit computes the expected public result.
2. **State transition:** the ledger state updates exactly as intended after a valid call.
3. **Privacy boundary:** the raw private witness is absent from public ledger state, circuit return values, disclosed values, serialized results, and other observable output. Test the actual contract behavior; do not satisfy this with a trivial string check alone.

Add more focused tests if needed. Keep tests deterministic and avoid real network dependencies.

Run the repository's test command, preferably:

```bash
npm test
```

In the final report, show the command, pass/fail status, number of passing tests, and a concise excerpt of the output.

## 3. GitHub Actions CI

Create or correct `.github/workflows/ci.yml`.

Requirements:

- Trigger on pushes to `main`.
- Trigger on pull requests.
- Use Node.js 22.
- Check out the repository.
- Install dependencies reproducibly with `npm ci` because a lockfile exists.
- Compile the Compact contract using the repository's script, normally `npm run compile`.
- Run the complete test suite with `npm test`.
- Run the production build with `npm run build` so TypeScript and frontend build failures are caught.
- Use only valid, current GitHub Actions syntax.
- Add caching only if it is safe for this project.

Do not invent a global `compact compile` command if `package.json` already defines the correct compile command.

Add this CI badge immediately below the README title:

```md
[![CI](https://github.com/EkinOnat/midnight-private-counter/actions/workflows/ci.yml/badge.svg)](https://github.com/EkinOnat/midnight-private-counter/actions/workflows/ci.yml)
```

Locally validate as much of the workflow as possible. Be explicit that the live badge can turn green only after the workflow has been pushed to GitHub and completed successfully.

## 4. Polish the dApp

Review the existing frontend and implement any missing behavior:

- Clear, actionable messages for wallet, network, proof-server, proof-generation, transaction, and contract-read errors.
- A visible loading indicator while proof generation or transaction submission is in progress.
- Prevention of accidental duplicate submissions while an operation is pending.
- Clear UI labels explaining which data is public, which input remains private, and what is proved without revealing it.
- A mobile-responsive layout with no horizontal overflow at common phone widths.
- Accessible controls, status text, and keyboard focus behavior.
- No private witness values in logs, UI state intended for display, URLs, persistent browser storage, or error messages.
- No avoidable console errors in the production build or in covered frontend flows.

Preserve the current visual identity and working behavior unless a change is necessary to meet these requirements.

Run:

```bash
npm run typecheck
npm run build
```

If `npm run build` already includes type checking, it is still acceptable to report both checks. Confirm the actual outcome in the final report.

## 5. Create `PROPOSAL.md`

Create `PROPOSAL.md` in the repository root with exactly these sections and placeholders. Do not fill in the user's answers:

```md
# Product Proposal

## What is the product, and who uses it?

[I WILL FILL THIS IN]

## Why Midnight specifically?

[I WILL FILL THIS IN - what does Midnight do that a transparent chain could not do well for this product?]

## Data Model

| Data Point | Type | Disclosed To |
|---|---|---|
| [example] | Public ledger | Everyone |
| [example] | Private witness | No one |

[I WILL FILL IN THE ROWS]

## Mainnet Feasibility

[I WILL FILL THIS IN - is this realistic to reach Mainnet by Level 6?]
```

## 6. Update `README.md`

Preserve useful, accurate Level 2 documentation, but organize the README so it contains all of the following sections in this order:

```md
# Midnight Private Counter

[CI badge]

> One-line description.

## Live Demo

[Production URL]

## Contract Address

| Network | Address |
|---|---|
| Preprod | `516b830d25b61b83abd63488618a8dc45e4aecc1a04da18377e467792bdeed62` |

## What This Does

## Privacy Model

- **PUBLIC:**
- **PRIVATE:**
- **PROVED without revealing:**

## Privacy Claim

Explain precisely what an on-chain observer can see and cannot see. Keep the claim consistent with the Compact contract and application code; do not overclaim privacy.

## Tech Stack

## Prerequisites

## Setup & Run Locally

Provide complete, tested, step-by-step commands.

## Run Tests

```bash
npm test
```

## CI/CD

Explain what the workflow checks and when it runs.

## Product Proposal

See [PROPOSAL.md](PROPOSAL.md).
```

The **Contract Address**, **Privacy Model**, and **Privacy Claim** sections are mandatory. Keep any other useful sections after the required ordered sections.

## 7. Prepare the one-minute demo checklist

In the final response, give me a practical one-minute recording plan with approximate timestamps that shows:

1. The full dApp flow: connect wallet -> call the circuit -> show the successful public result.
2. A terminal showing at least three passing tests.
3. The GitHub README showing a green CI badge.

Mention any services that must already be running, such as the local proof server, and any wallet/network preparation needed before recording.

## 8. Final verification and report

Before finishing:

1. Review the diff for accidental secrets, generated-file mistakes, placeholders outside `PROPOSAL.md`, and unrelated changes.
2. Run the Compact compilation, full test suite, type check, and production build using the actual package scripts.
3. Inspect `git status` and summarize every changed file.
4. Do not silently leave failing checks.

End with this checklist, using `[PASS]`, `[FAIL]`, or `[WARN]` and one short evidence note for every item:

```text
[ ] 3+ meaningful tests pass
[ ] CI/CD workflow is configured for push to main and pull requests
[ ] CI badge is present in README.md
[ ] Preprod contract address is present in README.md
[ ] Privacy Model and Privacy Claim are present in README.md
[ ] PROPOSAL.md has the required structure and untouched placeholders
[ ] Compact contract compiles successfully
[ ] Type checking passes
[ ] Production build completes with zero errors
[ ] Required project structure is present
[ ] No secrets or private witness values were added to tracked files
```

Also provide:

- Commands run and their results.
- Files changed and why.
- Any limitation that still requires a manual or external action.
- The one-minute demo plan.
- A suggested plan for splitting the Level 3 work into 10 meaningful commits. Do not fabricate or create empty commits.
- A reminder for me to complete the placeholders in `PROPOSAL.md`.
